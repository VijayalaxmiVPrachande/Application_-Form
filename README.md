# Application_-Form
This project is a simple Application Form System built using Spring Boot and MySQL. It allows users to submit form data through a web interface, which is processed by REST APIs and stored in a MySQL database using JPA/Hibernate. The project demonstrates backend development, database connectivity, and basic CRUD operations using Java and Maven.
